/* This is the BuckleScript configuration file. Note that this is a comment;
  BuckleScript comes with a JSON parser that supports comments and trailing
  comma. If this screws with your editor highlighting, please tell us by filing
  an issue! */

/* BuckleScript uses bsconfig as a compilation config file, but also as an
  anchoring point to know where the root of the project is. Since we're learning
  the fundamentals, we'll only be using bsconfig for the latter purpose. For the
  former purpose, see
  http://bucklescript.github.io/bucklescript/Manual.html#_bucklescript_build_system_code_bsb_code */

{
  "name" : "step7",
}
